region             = "{{ aws_region }}"
project_id         = "{{ project_id }}"
project_name       = "{{ project_name }}"
environment        = "{{ environment }}"
domain_name        = "{{ domain }}"  # Only if domain is provided
route53_zoneid     = "{{ route53_zone_id }}"
