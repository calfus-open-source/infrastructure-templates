region = "{{ aws_region }}"
project_id = "{{ project_id }}"
project_name = "{{ project_name }}"
source_code_repository = "{{ source_code_repository }}"

{% if source_code_repository == 'github' %}
github_access_token = "{{ github_access_token }}"
github_owner = "{{ github_owner }}"

{% if service_type == 'frontend-service' %}
  {% assign app_types = frontend_app_type %}
{% elsif service_type == 'backend-service' %}
  {% assign app_types = backend_app_type %}
{% else %}
  {% assign app_types = service_type %}
{% endif %}

appType = ["{{ app_types }}"]

{% endif %}

{% if domain %}
domain_name = "{{ domain }}"
{% endif %}
