provider "aws" {
    region     = "ap-south-1"
}

terraform {
    backend "s3" {
    }
}

module "vpc" {
  source      = "./modules/vpc"
  name        = "${var.project_name}-${var.environment}-${var.moduleName}-vpc"
  environment = var.environment
  cidr        = var.cidr

}
